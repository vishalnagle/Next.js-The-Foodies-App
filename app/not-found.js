
const NotFound = () => {
    return <main className="not-found">
        <h1>Not found</h1>
        <p>Unfortunately, we could not found the page or resource.</p>
    </main>
}

export default NotFound